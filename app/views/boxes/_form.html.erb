<%= form_for(box) do |f| %>
  <% if box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(box.errors.count, "error") %> prohibited this box from being saved:</h2>

      <ul>
      <% box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :image_data %>
    <%= f.text_area :image_data %>
  </div>

  <div class="field">
    <%= f.label :indicator %>
    <%= f.text_field :indicator %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
